<%# ヘッダー %>
<%= render "shared/header" %>
<%# ヘッダー %>

<div class="content-whole">
<%# サイドバー %>
<%= render "shared/side_ber"%>
<%# サイドバー %>

<%# メインバー %>

<div class="index-whole">
  <span class="form-title-g">贈った物の記録</h3></span>

    <% @gave_gifts.each do |gave_gift| %>
      <div class="gave_gifts">
        <div class="gave_gift">
    
        </div>
      </div>
    <% end %>

</div>
<%# メインバー %>
</div>

<%# フッター %>
<%= render "shared/footer" %>
<%# フッター %>